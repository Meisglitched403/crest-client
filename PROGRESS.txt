================================================================================
CREST-CLIENT  —  HYBRID RECORDER PROJECT STATUS
================================================================================
Project: crest-client (Minecraft 26.1.2 / Fabric mod)
Goal:    Record gameplay as a HYBRID — gameplay captured as re-simulated STATE
         (packets + ticks + inputs, tiny/lossless), GUI/HUD captured as PIXELS
         (menus can't be replayed). Single .crest timeline, seamless playback.

Session: "holaaaa amigosss"  (opencode session slug: witty-otter)

================================================================================
WHAT WE HAVE DONE
================================================================================

PHASE 0 — Diagnostics & plan
  - Located the original opencode session in ~/.local/share/opencode/opencode.db
    (session id ses_0bed33601ffeadSaCF1FAmhnRs, title "holaaaa amigosss").
  - Read the full conversation: the session was mid-work on crest-client's
    .crest recording feature (glassmorphic UI, recording/preview player).
  - Last bugs reported: .crest preview distorted, recording length mismatch
    (8s shown as 3s), backtick crash, recording crashes.
  - Agreed plan: pursue the HYBRID approach (state-replay + pixel capture for
    GUI), fixing the two pixel bugs along the way. User chose BOTH singleplayer
    and multiplayer; fallback = "go all-in on hybrid".

PHASE 1 — Pixel half: fixed distortion, duration, smoothness  [DONE]
  Files: Recorder.java, RecorderPlayer.java, RecorderExporter.java
  - DISTORTION: capture now reads the framebuffer the same way Minecraft's own
    Screenshot does (native ints, vertical flip, force alpha) and stores
    v3 frames; playback decodes via NativeImage.setPixelABGR + texture.upload().
    Verified end-to-end against MC source (writeToTexture does no Y-flip; blit
    maps texture-top -> screen-top) -> provably upright.
  - DURATION: playback advances by real per-frame TIMESTAMPS (frameTimestamps[])
    instead of an assumed fps; on-screen timer (fmt) also uses timestamps, so a
    25s clip shows 0:25 not 0:04.
  - SMOOTHNESS: root cause of ~10fps was ~24MB per-frame allocations on the
    RENDER thread (GC thrash). Fixed by:
      * capture does only mapBuffer + pooled memcpy on render thread (ZERO
        per-frame allocation there),
      * vertical-flip + deflate moved entirely to the writer thread,
      * raw GPU buffers pooled and reused.
  - Container version bumped 2 -> 3 (old broken files not mis-decoded).
  - RecorderExporter updated: skips the vflip filter for v3 (capture already
    flips) so MP4 export isn't double-flipped.

PHASE 2 — State half (recording): packets + ticks + meta  [DONE, compiles/builds]
  Files: StateRecorder.java (new), ConnectionMixin.java (new),
         ClientTickMixin.java (new), Recorder.java (wired),
         crest-client.mixins.json (registered)
  - Records gameplay as STATE into a sidecar file "<name>.crest.state"
    (magic "CRS1"), as timestamped records:
      * META   — seed(placeholder 0), dimension, player UUID, spawn, w/h
      * PKT_IN / PKT_OUT — every gameplay packet, encoded via
        GameProtocols.CLIENTBOUND_TEMPLATE / SERVERBOUND_TEMPLATE codecs,
        using the LIVE RegistryAccess so entity/block/registry packets serialize.
      * TICK   — one marker per client tick.
  - Mixins: Connection.channelRead0 (inbound) + Connection.send (outbound),
    gated to in-world PLAY phase; Minecraft.tick() for tick markers + first-tick
    META.
  - Fully defensive: every interception wrapped in try/catch and gated on
    level!=null && player!=null. A packet that fails to encode is skipped — it
    can NEVER crash the game or disturb the pixel recorder.
  - Wired into Recorder.start()/stop() so state + pixel recording run together.
  - Full `./gradlew build` (compile + remap + package) SUCCEEDS.

API research done (MC 26.1.2 specifics):
  - ResourceLocation renamed to Identifier (net.minecraft.resources.Identifier).
  - SimpleUnboundProtocol (clientbound) vs UnboundProtocol<_,_,Context>
    (serverbound) — different template types, handled separately.
  - Level has no getSeed() on client -> placeholder 0 (read from live world in
    Phase 3).
  - ConnectionProtocol enums have no direct codec accessor; use
    GameProtocols.CLIENTBOUND_TEMPLATE / SERVERBOUND_TEMPLATE.bind(...).
  - ClientPacketListener.registryAccess() for live registry during encode.

================================================================================
PROGRESS
================================================================================
  [DONE] Phase 0  : diagnostics + hybrid plan
  [DONE] Phase 1  : pixel recorder fixed (upright, timestamp-accurate, smooth)
  [DONE] Phase 2  : state RECORDING (packets+ticks+meta) -> .crest.state sidecar
  [DONE] Phase 3  : state PLAYBACK (re-simulation) + input capture
  [TODO] Phase 4  : unify segments into one seamless .crest timeline + player
  [TODO] Phase 5  : MP4 export from re-simulated frames; MP fidelity hardening

  [DONE] Bugfix   : streaming audio ON auto-falls-back to no-audio (ffmpeg death retry)
  [DONE] Bugfix   : capture FPS cap removed — GPU staging buffer now pooled/reused
  [DONE] Bugfix   : removed orphaned Crest Nametag font registration (red-dot nametag)
  [DONE] OPTIMIZE  : perf pass across hot paths (build-verified):
                       - EventBus.post: no per-post List.copyOf (A)
                       - ReplayTickMixin: reflected Methods cached once (M)
                       - StatePlayer.pollUpTo: reuses one list (L)
                       - ReplayBuffer: downscale moved off render thread to a
                         dedicated worker; bounds-check + per-pixel float math
                         removed from the inner loop (J)
                       - HUD modules cache formatted text + Component, rebuild
                         only on value change: FpsModule, CoordsModule,
                         SpeedometerModule, ArmorHudModule, PotionHudModule,
                         ItemCounterModule, WaypointsModule (+ Waypoint label
                         cache), RecorderIndicator, StreamerIndicator (B-I)
                       - StateRecorder: reuses scratch Netty/FriendlyByteBuf in
                         encodePacket + builders + enqueue ByteBuffer (K)
                       - ZoomModule: caches singleton ref (no Map.get/cast per
                         mouse-turn) (O)

  [DONE] NOVEL    : Adaptive Capture Frame-Budget (ACFB) — the "nobody does this"
                     optimization. A self-measuring scheduler at GameRenderer.render
                     HEAD/TAIL measures real per-frame render time (EMA) and only
                     allows the capture/readback pipeline to run when there is spare
                     frame budget; when gameplay FPS sags it SKIPS capture that frame
                     and backs the capture interval off, so gameplay FPS is always
                     protected. Recording clients normally capture unconditionally
                     (stealing FPS from the game) — ACFB inverts that. Implemented
                      as core/FrameBudget.java + core/mixin/RenderBudgetMixin.java,
                      wired into FrameCapture.onRenderEnd(); safe mode (worst case:
                      capture FPS dips under load, gameplay never affected).

  [DONE] MEASURE  : Perf HUD module (id "perf", HUD category, default ON) — turns
                     "I guess it's faster" into hard numbers. Shows: live FPS
                     (color-coded), real frame time in ms (FrameBudget EMA, red when
                     over the monitor's refresh budget), capture state (RUN vs
                     SKIP budget/interval, from ACFB), Block LOD active tier (reads
                     BlockLodModule.active() + camera-distance vs tier thresholds),
                     and render distance. Text is cached + rebuilt only on value
                     change (mirrors FpsModule). Toggling in the Crest menu reloads
                     the HUD automatically via CrestModules.renderCacheDirty.
                     - core/PerfModule.java (new)
                     - core/FrameBudget.java: added read-only lastCaptureDecision()
                     - core/CrestClient.java: registered PerfModule

  [DONE] PERF    : Adaptive Render Distance — compounds with Block LOD. Watches the
                     real frame-time EMA (FrameBudget) every client tick and shrinks
                     options.renderDistance() when over the target-FPS budget, grows
                     it back when there is headroom. Debounced (adjust cooldown in
                     ticks) so the value never oscillates frame-to-frame. Settings:
                     Enabled (default OFF), Min/Max distance (sections), Target FPS,
                     Adjust cooldown (ticks). On enable it captures the user's current
                     distance and clamps into [min,max]; on disable it restores the
                     saved distance. Verifiable live via the Perf HUD "RD" line.
                     - core/AdaptiveRenderDistanceModule.java (new)
                     - core/CrestClient.java: registered module

  [DONE] NOVEL    : Tiered Block LOD — the "drastic, quality-preserving" optimization.
                     Hooks SODIUM'S OWN block mesher (BlockRenderer) — which owns block
                     geometry whenever Sodium is present — and cheapens how DISTANT
                     blocks are built into section meshes, while near blocks stay at
                     full quality/resolution. No cheat client does this (they only
                     toggle Sodium's settings); this substitutes WHAT geometry is
                     produced based on camera distance, inside the mesher.
                     - core/BlockLodModule.java: Crest module (default OFF), with
                       configurable per-tier distance thresholds (sections).
                     - core/mixin/SodiumBlockLodMixin.java (separate required:false
                       mixin config, so it no-ops gracefully if Sodium is absent):
                         * renderModel HEAD: compute block distance from cached camera
                           pos (FrameBudget volatile) -> tier 0/1/2/3.
                         * prepareAoInfo: tier>=1 cancels -> flat lighting (no per-vertex
                           AO, the most expensive mesher op). Zero visible loss at range.
                         * shouldDrawSide: tier>=2 aggressively culls faces adjacent to
                           any non-air neighbor (interior faces never visible).
                         * processQuad: tier>=1 collapses cutout/translucent quads to the
                           SOLID layer (drops expensive transparency passes at distance).
                     - build.gradle: compileOnly on local Sodium jar (not bundled).
                     Adds nothing to near-block quality; cuts far-field mesh-build cost
                     (expected +20-50% FPS typical, +60-100%+ in heavy/complex worlds).
                     


================================================================================
WHAT IS LEFT TO DO
================================================================================

PHASE 3 — Playback re-simulation + input capture  [DONE, compiles/builds]
   Files: StatePlayer.java (new), ReplayTickMixin.java (new),
          ConnectionBlockMixin.java (new), MouseHandlerAccessor.java (new),
          StatePlayerHolder.java (new), MouseInputMixin.java (new),
          KeyboardHandlerMixin.java (new), StateRecorder.java (extended),
          crest-client.mixins.json (updated)
   - INPUT capture: KeyboardHandlerMixin records keyPress(win,key,scancode,action,mods)
     as TYPE_KEY records. MouseInputMixin records onPress → TYPE_MOUSE_BTN,
     onScroll → TYPE_MOUSE_SCROLL, turnPlayer → TYPE_MOUSE_MOVE.
     All gated to in-world (level!=null, screen==null), wrapped in try/catch.
   - StateRecorder extended with 4 new record types (KEY, MOUSE_BTN, MOUSE_SCROLL,
     MOUSE_MOVE) and build/send helpers using FriendlyByteBuf encoding.
   - StatePlayer: reads .crest.state (magic "CRS1"), parses all records into typed
     list (Meta, PKT_IN, PKT_OUT, TICK, KEY, MOUSE_BTN, MOUSE_SCROLL, MOUSE_MOVE),
     provides pollUpTo(timeUs) for replay, and decodes packets via the same
     GameProtocols codecs used for encoding.
   - ReplayTickMixin: injects at Minecraft.tick HEAD. When StatePlayerHolder.active
     is set, it polls due records up to elapsed replay time. PKT_IN decoded and
     fed into ClientPacketListener via raw-cast handle(). MOUSE_MOVE sets
     accumulatedDX/DY via MouseHandlerAccessor then calls handleAccumulatedMovement().
     KEY replay uses lambda$setup$4 reflection trampoline.
   - ConnectionBlockMixin: suppresses Connection.tick() during replay to prevent
     double-feeding (real connection packets + injected replay packets).
   - MouseHandlerAccessor: exposes accumulatedDX/accumulatedDY for replay rotation.
   - StatePlayerHolder: singleton bridge (active, startTimeMs, elapsedUs/durationUs).
   - Full `./gradlew build` SUCCEEDS.
   NOTES:
     - KEY replay uses reflection trampoline (lambda$setup$4); may need refinement
       if the lambda signature differs across MC versions.
     - MOUSE_MOVE sets both accumulatedDX + accumulatedDY in one call then processes
       both via handleAccumulatedMovement(). Verified against MC 26.1.2 decompiled
       MouseHandler API (accumulatedDX/DY are private double fields, turnPlayer(double)
       is single-arg, handleAccumulatedMovement() is public).
     - PKT_IN injection uses unchecked Packet<PacketListener> cast matching
       Connection.genericsFtw pattern (same cast the real Connection does).
     - Minecraft.getConnection() returns ClientPacketListener (not Connection!) in
       MC 26.1.2. Adjusted all code to use this API.
   KNOWN LIMITATIONS (MVP):
     - Re-simulation does NOT yet create a synthetic world — you must replay while
       connected to a world (existing singleplayer/multiplayer). The mixin feeds
       recorded packets into the live connection; world state updates accordingly.
       Full world-from-scratch replay is Phase 4 stretch.
     - MOUSE_BTN/MOUSE_SCROLL are recorded but not replayed (player movement via
       MOUSE_MOVE is sufficient for rotational fidelity).
     - Multiplayer replay fidelity (other players, chunk hashing, RNG) still stretch.

PHASE 4 — Unified hybrid timeline
  - Single .crest container with interleaved STATE segments (gameplay) and
    PIXEL segments (GUI-open periods, tagged by [t_start,t_end]).
  - Player switches per period: re-simulate gameplay, blit pixel frames for GUI.
  - Add the previously-requested FILE-SYSTEM BROWSER to the preview player
    (earlier request: "need a file system there").

PHASE 5 — Export & hardening
  - RecorderExporter: render off re-simulated frames (or pixel frames for GUI
    spans).
  - Multiplayer state-replay fidelity hardening (stretch).

ALSO STILL OPEN (from original session, not yet addressed)
  - Backtick crash ("game crashed as soon as I pressed backtick").
  - General recording-crash stability ("it crashed ... Process finished with
    non-zero exit value 25").
  - These should be investigated once the hybrid pipeline is validated.

================================================================================
VERIFICATION STATUS
================================================================================
  Verified : code compiles, full build + remap + jar packaging succeeds.
  Verified : orientation math proven correct against MC Screenshot pipeline.
  NOT yet  : in-game runtime testing (no display in this environment).
              User must confirm: (1) pixel playback upright + smooth + correct
              timer, (2) .crest.state sidecar is created and grows on record,
              (3) no crashes / smooth gameplay while recording.

  [DONE] UX       : Shared, fully-customizable HUD background (fixes the old
                     misalignment bug where right-anchored modules filled at the
                     wrong x,y). New core/HudBackground.java exposes per-module
                     settings: BG Enabled, BG Color, BG Opacity (0-255), BG Style
                     (Filled / Border / Frosted). Frosted = cheap glass look (no
                     real world-blur, which is too expensive for an in-world HUD).
                     Every HUD module computes its own anchored rect and calls
                     bg.draw(g, rx, ry, w, h) so bg ALWAYS aligns with content.
  [DONE] UX       : Improved + fixed existing HUD modules:
                     - CoordsModule: now uses HudBackground; fields XYZ / Facing /
                       Biome are individually toggleable; right-anchor bg FIXED.
                     - FpsModule: added rolling min/avg/max over a 1s window; bg.
                     - PerfModule: uses HudBackground.
                     - ArmorHudModule: now draws a panel background; durability
                       modes expanded to Bar+% / % only / Num / Bar only / Bar+Num;
                       "Show Held Item" toggle; bg.
  [DONE] UX       : New modules (all in HUD category, toggleable in the Crest menu,
                     each with its own customizable background):
                     - BiomeModule (id "biome", default OFF): standalone biome
                       readout split from Coordinates; toggles for Name / Registry
                       ID / Coordinates; themable bg.
                     - CpsModule (id "cps", default OFF): left/right clicks-per-second
                       with a small rolling bar graph. Click feed: MouseInputMixin
                       now forwards button-press edges to CpsTracker (unconditionally;
                       StateRecorder feed preserved).
                     - TogglesModule (id "toggles", default OFF): interactive panel
                       listing every other module with on/off state; left-click a row
                       to toggle it in-game. Click path: MouseInputMixin -> HudClickBus
                       -> ClickableHud (reflected MouseHandler.xpos/ypos, scaled).
                     New: core/HudBackground.java, core/BiomeModule.java,
                     core/CpsModule.java, core/CpsTracker.java, core/TogglesModule.java,
                     core/ClickableHud.java, core/HudClickBus.java.
                      MouseInputMixin extended (CPS + HUD click dispatch).
                      Registered all in CrestClient.java. Build passes.

  [DONE] UX       : Bongo Cat fully reworked to replicate the bongo-cat-obs plugin.
                     The old version used static PNG sprites (body.png / paw_*.png)
                     that did not match the iconic look ("not proper"). Replaced with
                     a PROCEDURAL vector cat drawn via new core/bongocat/DrawUtil.java
                     (rect / circle / ellipse / triangle / roundRect scanline fills
                     on GuiGraphicsExtractor.fill): rounded "loaf" body, circular head
                     with triangle ears + pink inner ears, two eyes, and two front paws
                     that RISE (lift) when keys/mouse are pressed (driven by the
                     existing SpringPaw physics). Optional virtual keyboard beside the
                     cat shows WASD (blue), space/shift/ctrl, and LMB/RMB, lighting up
                      yellow on press — same signature as bongo-cat-obs.
                      - (later REVERTED — see below)

  [DONE] UX       : Bongo Cat REBUILT faithfully to the real bongo-cat-obs plugin
                     (user correction: "use the actual bongo cat images"). Pulled the
                     real plugin from ~/Downloads/bongocat-2.0.4.zip (skins/default:
                     cat-rest/cat-left/cat-right.png) and studied its source:
                       * 3 sprites (rest = both paws down, left = left paw up,
                         right = right paw up), displayed rotated -13deg.
                       * right paw = mouse; left paw = keystrokes.
                       * idle "breathing" sine stretch; only WASD/Shift/Ctrl matter.
                     Implementation:
                       * Baked the 3 sprites rotated -13deg with ImageMagick into
                         src/main/resources/assets/crest-client/bongocat/*.png
                         (226x170) so they blit axis-aligned like the plugin shows.
                       * BongoCatOverlay: blits correct sprite per state with a subtle
                         vertical breathing scale; LEFT paw -> cat-left when WASD/
                         Shift/Ctrl held; RIGHT paw -> cat-right follows the cursor
                         smoothly (smoothed offset, lerp) and shows while the mouse
                         moved recently. Uses GuiGraphicsExtractor.blit(Identifier,...).
                       * InputTracker: polls only W/A/S/D/LeftShift/RightShift/LeftCtrl
                         + GLFW cursor pos (gui-scaled) for the right-paw follow.
                       * BongoCatModule now extends HudModule (not a raw CrestModule +
                         HudElementRegistry), so it is positioned/dragged via the
                         STANDARD Crest HUD editor (F7) — no separate drag GUI, as
                         requested. Default bottom-center.
                       * Removed: BongoCatEditScreen, VirtualKeyboard, DrawUtil,
                         SpringPaw, the Left-Alt editor keybind. BongoCatConfig trimmed
                         to just `scale`. Added `scale` setting option implicitly.
                     Build passes; sprites packaged in jar.

  [FIX]     UX       : Bongo Cat was invisible ("I don't see anything"). Root cause:
                     custom PNG textures are NOT auto-loaded by GuiGraphicsExtractor.blit
                     — they must be registered with Minecraft's TextureManager first
                     (same as ui/Panel does). Added BongoCatTextures.ensure() which
                     loads cat-rest/cat-left/cat-right from the jar resources into
                     registered textures via DynamicTexture(Supplier<String>, NativeImage)
                     + TextureManager.register, and BongoCatOverlay now blits them with
                     RenderPipelines.GUI_TEXTURED (tint 0xFFFFFFFF). Also fixed
                     BongoCatModule.getWidth/getHeight to read TEX_W/TEX_H from
                     BongoCatTextures. Build passes.

================================================================================
