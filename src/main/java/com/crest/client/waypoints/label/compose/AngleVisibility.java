package com.crest.client.waypoints.label.compose;

/** Controls when/how a label element is visible relative to the camera. */
public enum AngleVisibility {
    ALWAYS,
    FACING_ONLY,
    NOT_FACING
}
